{
  "scripts": {
    "build": "token-transformer tokens.json tokens/output.json && npx style-dictionary build"
  },
  "devDependencies": {
    "style-dictionary": "^3.7.1"
  },
  "dependencies": {
    "husky": "^8.0.2",
    "token-transformer": "^0.0.27"
  }
}
